# Data Directory

This folder contains the project's datasets.

- `processed/` stores curated and derived data used by the application and experiments.
- `raw/` keeps the original source datasets and download artifacts.

Raw dataset files are intentionally excluded from Git, but the folder structure is kept visible in the repository.
